export async function navbar_initAsync()
{
    /*
    document.getElementById("toggle-settings").addEventListener("click", () => {
        const settings = document.getElementById("settings");
        if (settings.classList.contains("is-active")) settings.classList.remove("is-active");
        else settings.classList.add("is-active");
    });
    document.getElementById("toggle-volume").addEventListener("click", () => {
        const settings = document.getElementById("volume-container");
        if (settings.classList.contains("is-active")) settings.classList.remove("is-active");
        else settings.classList.add("is-active");
    });
    */
}